stages:
  - build
  - test

build:
  stage: build
  script:
    - cd streetSoccerManager/streetSoccerManager/
    - ./gradlew build -x test
    - ./gradlew.bat --stop

test:
  stage: test
  script:
    - cd streetSoccerManager/streetSoccerManager/
    - ./gradlew test
    - ./gradlew.bat --stop